name: Merge Rules

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  merge:
    runs-on: ubuntu-latest
    # 提前开启 Node 24 运行环境以消除警告
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        # 升级到 v5 以原生支持 Node 24
        uses: actions/checkout@v5

      - name: Set date
        run: echo "DATE=$(date +'%Y-%m-%d')" >> $GITHUB_ENV

      - name: Download sgmodule
        run: |
          curl -L \
          https://raw.githubusercontent.com/QingRex/LoonKissSurge/main/Surge/%E5%B9%BF%E5%91%8A%E5%B9%B3%E5%8F%B0%E6%8B%A6%E6%88%AA%E5%99%A8.sgmodule \
          -o module.sgmodule

      - name: Download conf rules
        run: |
          curl -L https://raw.githubusercontent.com/olocn/rule/refs/heads/master/surge/reject-drop.txt -o ruleA.txt
          curl -L https://raw.githubusercontent.com/TG-Twilight/AWAvenue-Ads-Rule/main/Filters/AWAvenue-Ads-Rule-Surge-RULE-SET.list -o ruleB.list

      - name: Extract + merge + optimize rules
        run: |
          set -e

          # 1️⃣ 提取 sgmodule 的 [Rule]
          awk '
            /^\[Rule\]/ {flag=1; next}
            /^\[/ {flag=0}
            flag {print}
          ' module.sgmodule > sg_rules.tmp

          # 2️⃣ 清洗：去除所有后缀、空格并去重
          # 核心改动：正则匹配各种 reject 变体并删除
          cat sg_rules.tmp ruleA.txt ruleB.list \
            | sed '/^\s*$/d' \
            | grep -v '^#' \
            | sed -E 's/,(REJECT|reject|REJECT-DROP|reject-drop)//gI' \
            | sed 's/,extended-matching//g; s/,pre-matching//g; s/,no-resolve//g' \
            | sed 's/[[:space:]]//g' \
            | awk '!seen[$0]++' \
            > rules.cleaned

          # 3️⃣ 优化：DOMAIN-SUFFIX 覆盖 DOMAIN
          # 如果存在 DOMAIN-SUFFIX,jpush.io，则删除 DOMAIN,jpush.io
          awk -F',' '
          {
            rules[NR] = $0
            type[NR]  = $1
            value[NR] = $2
            if ($1 == "DOMAIN-SUFFIX") suffix[$2] = 1
          }
          END {
            for (i = 1; i <= NR; i++) {
              if (type[i] == "DOMAIN") {
                matched = 0
                for (s in suffix) {
                  # 匹配完全相等 (jpush.io) 或子域名 (.jpush.io)
                  if (value[i] == s || value[i] ~ ("\\." s "$")) {
                    matched = 1
                    break
                  }
                }
                if (matched) skip[i] = 1
              }
            }
            for (i = 1; i <= NR; i++) {
              if (!skip[i]) print rules[i]
            }
          }' rules.cleaned > rules.optimized

          # 4️⃣ 生成 Surge 格式文件 (根据要求：去除所有 ,REJECT)
          SURGE_OUT="ads-latest.txt"
          echo "# Auto merged Surge rules (${DATE})" > "$SURGE_OUT"
          awk -F',' '
          {
            split($2, d, ".");
            root = d[length(d)-1] "." d[length(d)];
            print root "\t" $0
          }' rules.optimized \
            | sort -k1,1 \
            | cut -f2- >> "$SURGE_OUT"

          # 5️⃣ 生成 MosDNS 格式文件
          MOSDNS_OUT="ads-mosdns.txt"
          echo "# Auto merged MosDNrules (${DATE})" > "$MOSDNS_OUT"
           awk -F',' '
          {
            if ($1 == "DOMAIN") print "full:" $2
            else if ($1 == "DOMAIN-SUFFIX") print "domain:" $2
            else if ($1 == "DOMAIN-KEYWORD") print "keyword:" $2
          }' rules.optimized >> "$MOSDNS_OUT"

          # 6️⃣ 生成 Quantumult X 格式文件
          QX_OUT="ads-qx.snippet"
          echo "# Auto merged Quantumult X rules (${DATE})" > "$QX_OUT"
          awk -F',' '
          {
            if ($1 == "DOMAIN") print "host, " $2 ", reject"
            else if ($1 == "DOMAIN-SUFFIX") print "host-suffix, " $2 ", reject"
            else if ($1 == "DOMAIN-KEYWORD") print "host-keyword, " $2 ", reject"
            else if ($1 == "IP-CIDR") print "ip-cidr, " $2 ", reject"
            else if ($1 == "IP-CIDR6") print "ip6-cidr, " $2 ", reject"
          }' rules.optimized >> "$QX_OUT"

      - name: Commit result
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add ads-latest.txt ads-mosdns.txt ads-qx.snippet
          git commit -m "Update rules and fix Node deprecation (${DATE})" || echo "No changes"
          git push
